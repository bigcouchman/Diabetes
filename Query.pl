?- under_control(Sam).
# What is the output of the query above?

?- has_type_1(Alisha).
# What is the output of the query above?

?- type(none).
# What is the output of the query above?

?-advice(John, "Monitor glucose level").
# What is the output of the query above?

